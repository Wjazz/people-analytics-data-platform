# =============================================================================
# Bourbaki Engine — Terraform Variables (EJEMPLO)
#
# INSTRUCCIONES:
#   1. Copiar este archivo:  cp terraform.tfvars.example terraform.tfvars
#   2. Reemplazar los valores placeholder con credenciales reales
#   3. NUNCA commitear terraform.tfvars al repositorio
#
# Alternativa segura: Exportar como variables de entorno:
#   export TF_VAR_db_user="bourbaki_admin"
#   export TF_VAR_db_password="$(openssl rand -base64 24)"
# =============================================================================

project_id = "maverick-data-482905"
region     = "us-central1"

# --- Cloud SQL ---
db_name     = "bourbaki_db"
db_user     = "REEMPLAZAR_CON_USUARIO_REAL"
db_password = "REEMPLAZAR_CON_PASSWORD_SEGURO_MIN_16_CHARS"
db_tier     = "db-f1-micro"  # Cambiar a "db-custom-2-7680" para producción
